# SIH AI Tutor — SOAIDEATHON S28

## Folders
- `frontend/` — React app (chat, practice quiz, progress dashboard)
- `backend/` — Python API + RAG pipeline + database
- `docs/` — API contract and shared notes

## Setup
### Frontend
cd frontend
npm install
npm run dev

### Backend
(instructions go here once backend setup begins)